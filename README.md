# ST mDNS Server

这是一个用于 Android 的简单 mDNS (Multicast DNS) 服务器应用。它允许你在局域网内通过主机名（例如 `sillytavern.local`）而不是 IP 地址来访问你的设备服务。

## ✨ 功能特性

*   **mDNS 广播**: 注册并广播 `.local` 域名服务，支持自定义主机名和端口。
*   **网络优先级配置**: 允许自定义网络接口的优先级（例如优先使用 WLAN 接口），确保服务绑定到正确的 IP 地址。
*   **Termux 集成**: 提供一键启动 Termux 指令的功能，方便快速启动相关服务（如 SillyTavern）。
*   **后台保活**: 使用前台服务和唤醒锁，确保在后台或息屏时服务持续运行。
*   **多语言支持**: 支持简体中文和英文。

## 🚀 使用说明

### mDNS 服务
1.  打开应用，在主页点击 "启动服务"。
2.  默认服务名为 `sillytavern.local`，端口为 `8080`。
3.  你可以在 "设置" 页面修改服务名称和端口。

### 网络接口优先级
默认情况下，应用会自动选择合适的网络接口。如果你有多张网卡（如同时开启了 Wi-Fi 和热点），你可以设置优先级：
1.  进入 "设置" -> "网络设置" -> "接口优先级"。
2.  点击 "添加优先级规则" 添加接口名称前缀（例如 `wlan`, `eth`, `tun`）。
3.  使用上下箭头调整优先级顺序。应用会优先使用匹配列表顶端规则的接口。

### Termux 一键启动
此功能允许你通过点击按钮直接在 Termux 中运行命令（默认为 `st`）。
**首次配置步骤：**
1.  确保你已安装 Termux 应用。
2.  在 "设置" 中开启 "显示启动按钮"。
3.  你需要允许外部应用调用 Termux。请打开 Termux，运行以下命令（应用内弹窗也会提示）：
    ```bash
    mkdir -p ~/.termux && echo "allow-external-apps=true" >> ~/.termux/termux.properties && termux-reload-settings
    ```
4.  在应用主页点击 "启动 Termux 指令"，确认已完成配置即可使用。

## 🛠️ 构建说明

本项目使用 Gradle 构建。

**先决条件:**
*   JDK 11 或更高版本
*   Android SDK

**编译命令:**

```bash
# 获取源码
git clone https://github.com/kineks/mdnsserver.git
cd mdnsserver

# 赋予 gradlew 执行权限
chmod +x gradlew

# 编译 Debug 版本
./gradlew assembleDebug

# 编译 Release 版本
./gradlew assembleRelease
```

编译产物位于 `app/build/outputs/apk/` 目录下。

## 📄 许可证

[MIT License](LICENSE)
